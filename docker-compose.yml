version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    restart: always
    ports:
      - "6667:6667"
    networks:
      - default

networks:
  default:
    driver: bridge

volumes:
  data:
    driver: local